pipeline:
  name: success-pipeline

stages:
  - name: build
    jobs:
      - name: build-echo
        steps:
          - run: echo "Build successful"

  - name: test
    jobs:
      - name: test-echo
        steps:
          - run: echo "Tests passed"
