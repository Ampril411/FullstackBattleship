pipeline:
  name: fail-pipeline

stages:
  - build
  - test

build_job:
  - stage: build
  - script:
      - echo "Build step"

test_job:
  - stage: test
  - script:
      - echo "About to fail"
      - exit 1
